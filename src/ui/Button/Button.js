import React from 'react';

const Button = (props) => {
    
    return (
        <button style={{backgroundColor: props.color}}>
            Click me
        </button>
    )
}

export default Button;